
import React from 'react';
import { render, screen } from '@testing-library/react';
import App from './App';

/*
//test('renders learn react link', () => {
test('renders Task List in Banner component', () => {
  render(<App />);
  
  //const linkElement = screen.getByText(/Task List/i);
  //expect(linkElement).toBeInTheDocument();
}); 
*/




 // err    ● renders learn react link
  //const linkElement = screen.getByText(/Task List/i, { selector: 'div.banner', exact: false });
  //const linkElement = screen.getByText(/Task List/i, { selector: 'div[class="Banner"]' });
  
/*
import React from 'react';
import { render, screen } from '@testing-library/react';
import { MemoryRouter } from 'react-router-dom'; // Import MemoryRouter
import App from './App';

test('renders learn react link', () => {
  render(
    // Wrap your component with MemoryRouter
    <MemoryRouter>
      <App />
    </MemoryRouter>
  );

  // Continue with your test
  const linkElement = screen.getByText(/learn react/i);
  expect(linkElement).toBeInTheDocument();
});
*/


/*
import React from 'react';
import { render, screen } from '@testing-library/react';
import App from './App';

test('renders learn react link', () => {
  render(<App />);
 
  // err    ● renders learn react link
  const linkElement = screen.getByText(/Task List/i);
  expect(linkElement).toBeInTheDocument();
});
*/

 